# Copy to .env and fill in from your Supabase project settings (API).
# The publishable key is safe in a browser bundle; row access is enforced by
# RLS, not by hiding this key. Never put the service_role / secret key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
